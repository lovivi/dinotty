<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        values-night override.
        The base AppTheme (parent=Theme.Material.Light) already paints the dark
        surface via windowBackground/statusBarColor/navigationBarColor, so the
        dark variant is fully covered by values/styles.xml.
        This file exists to ensure resource resolution on night-mode devices
        and to provide a future-friendly override point.
    -->
    <style name="AppTheme" parent="android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/surface</item>
        <item name="android:statusBarColor">@color/surface</item>
        <item name="android:navigationBarColor">@color/surface</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:colorAccent">@color/primary</item>
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorPrimaryDark">@color/surface</item>
        <item name="android:textColorPrimary">@color/on_surface</item>
        <item name="android:textColorSecondary">@color/on_surface_variant</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>
</resources>